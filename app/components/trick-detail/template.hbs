<h1>{{trick.title}}</h1> <br> <p><img class="materialboxed" src={{trick.image1}}></p>
   steps: {{trick.content1}} <br>{{trick.content2}}
   <p><img class="materialboxed" src={{trick.image2}}></p>

   <iframe width="620" height="515" src= {{trick.video}}>
</iframe>

<button class="btn btn-danger"{{action 'delete' trick}}>Delete</button>
